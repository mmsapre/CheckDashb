:root {
    font-family: Inter, Segoe UI, Arial, sans-serif;
    color: #1b2432;
    background: #f3f5f9;

    --navy: #0e1726;
    --navy-2: #20365f;
    --red: #d64545;
    --amber: #e08e0b;
    --green: #279e5f;
    --border: #e6e9ef;
    --muted: #6b7686;
    --panel: #ffffff;
}

* {
    box-sizing: border-box;
}

body {
    margin: 0;
}

.app-header {
    padding: 16px 26px;
    color: white;
    background:
        linear-gradient(120deg, #0e1726, #20365f);
}

.app-header h1 {
    margin: 0;
    font-size: 19px;
}

.app-header p {
    margin: 4px 0 0;
    color: #aebcd4;
    font-size: 12px;
}

.tabs {
    display: flex;
    gap: 3px;
    padding: 0 26px;
    overflow-x: auto;
    background: #12203a;
}

.tabs a {
    padding: 12px 16px;
    color: #b9c6dc;
    text-decoration: none;
    white-space: nowrap;
    border-bottom: 3px solid transparent;
}

.tabs a.active {
    color: white;
    border-bottom-color: #4f9cf0;
}

.dashboard-layout {
    display: grid;
    grid-template-columns: 255px minmax(0, 1fr);
    gap: 18px;
    max-width: 1550px;
    margin: auto;
    padding: 16px 22px 60px;
}

.filter-panel {
    position: sticky;
    top: 12px;
    align-self: start;
    padding: 16px;
    max-height: calc(100vh - 24px);
    overflow: auto;
    background: white;
    border: 1px solid var(--border);
    border-radius: 12px;
}

.filter-panel h3 {
    margin: 0;
}

.filter-hint,
.hint {
    color: var(--muted);
    font-size: 12px;
}

.filter-group {
    padding: 12px 0;
    border-top: 1px solid var(--border);
}

.filter-title {
    display: block;
    margin-bottom: 7px;
    color: var(--muted);
    font-size: 11px;
    font-weight: 700;
    text-transform: uppercase;
}

.checkbox-row {
    display: flex;
    gap: 8px;
    align-items: center;
    margin: 6px 0;
    font-size: 13px;
}

.filter-panel select {
    width: 100%;
    padding: 8px;
    border: 1px solid var(--border);
    border-radius: 7px;
}

.reset-button {
    width: 100%;
    padding: 9px;
    border: 1px solid var(--border);
    border-radius: 8px;
    cursor: pointer;
}

.dashboard-main {
    min-width: 0;
}

.hero-panel {
    display: grid;
    grid-template-columns: 1.1fr 0.85fr 0.9fr;
    gap: 22px;
    padding: 22px 24px;
    color: white;
    background:
        linear-gradient(120deg, #12203a, #2b4a7e);
    border-radius: 16px;
}

.eyebrow {
    margin-bottom: 10px;
    font-size: 11px;
    letter-spacing: 1px;
    text-transform: uppercase;
}

.hero-ratio-value {
    font-size: 52px;
    font-weight: 750;
}

.hero-trend {
    margin-top: 10px;
    font-weight: 600;
}

.hero-kpi-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 10px;
}

.kpi {
    padding: 14px;
    background: white;
    border: 1px solid var(--border);
    border-radius: 12px;
}

.kpi.compact {
    color: white;
    background: rgba(255, 255, 255, 0.08);
    border-color: rgba(255, 255, 255, 0.12);
}

.kpi-label {
    color: var(--muted);
    font-size: 11px;
    text-transform: uppercase;
}

.compact .kpi-label {
    color: #d6dfef;
}

.kpi-value {
    margin-top: 4px;
    font-size: 23px;
    font-weight: 700;
}

.kpi-description {
    margin-top: 3px;
    color: var(--muted);
    font-size: 11px;
}

.lens-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 12px;
    margin: 16px 0;
}

.chart-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
}

.card {
    padding: 16px;
    background: white;
    border: 1px solid var(--border);
    border-radius: 12px;
}

.card h2 {
    margin: 0;
    font-size: 15px;
}

.insights {
    margin-top: 16px;
}

.insights li {
    margin: 6px 0;
    font-size: 13px;
}

.loading,
.error,
.placeholder {
    padding: 40px;
}

@media (max-width: 1050px) {
    .dashboard-layout {
        grid-template-columns: 1fr;
    }

    .filter-panel {
        position: static;
    }

    .hero-panel {
        grid-template-columns: 1fr;
    }

    .lens-grid,
    .chart-grid {
        grid-template-columns: 1fr;
    }
}
